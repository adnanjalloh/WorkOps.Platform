# Dependency repair verification — 11 September 2026

This report records local verification of dependency and application sources at commit
`cbb58f1ed9a029ca6f6350342430165d886e4138`, before this documentation-only follow-up.
The host was macOS arm64 with the repository-pinned .NET SDK `10.0.400` and Docker `29.4.3`.
It supplements the [previous hosted report](2026-09-09.md); it does not replace that report's
commit-specific CI, container-scan, or full-stack evidence.

## Change verified

- OpenTelemetry exporter, hosting, ASP.NET Core, HTTP, and runtime packages are aligned at `1.18.0`.
- StackExchange.Redis and its RESPite dependency are `3.1.31`.
- Both CodeQL steps are pinned to the same `4.37.9` release commit.
- Five affected lockfiles include the API, infrastructure, architecture, functional, and integration
  dependency graphs. The previously merged MSTest adapter/framework `4.4.0` update is included.

## Results

Locked restore reproduced `NU1004` before regeneration. After a solution-wide
`--force-evaluate -p:RestoreLockedMode=false` restore, locked restore passed for all nine projects.
Formatting verification passed and the Release build reported zero warnings and zero errors.

| Suite | Passed | Failed | Skipped |
| --- | ---: | ---: | ---: |
| Architecture | 8 | 0 | 0 |
| Functional | 21 | 0 | 0 |
| Integration | 24 | 0 | 0 |
| Unit | 53 | 0 | 0 |
| Total | 106 | 0 | 0 |

The suites use real PostgreSQL, Redis, and RabbitMQ containers where specified by the
[testing strategy](../testing.md). ReportGenerator `5.5.11` reported 90.3% line coverage
(7,653/8,466) and 48.9% branch coverage (599/1,223). The 70% line and 35% branch gates passed;
the gate's independently rounded output was 90.4% and 49.0%.

The NuGet audit, including transitive dependencies, reported no vulnerable packages from
`https://api.nuget.org/v3/index.json` on this date. Relative file links in the changed documentation
and the Dependabot YAML were checked locally.

## Reproduction and limits

Use the [dependency-update procedure](../testing.md#dependency-updates), then the coverage commands
in the same guide. Preserve locked restore in CI and release builds.

These are local regression and dependency-audit results. GitHub's required `verify`, `Analyze C#`,
and `review` checks remain separate merge gates; local results do not claim that CodeQL, a
container scan, or the complete Compose demo ran locally. The published `v0.1.0` image remains a
separate historical release. No production deployment or collector-backend validation is implied.
